# REGENERATE_PROMPT

- Source: `src/commands/thinkback/thinkback.tsx`
- Symbol: `REGENERATE_PROMPT`
- Line: 386
- Kind: `variable`
- Extraction: `text`

## Prompt

```text
Use the Skill tool to invoke the "thinkback" skill with mode=regenerate to create a completely new Claude Code year in review animation from scratch. Delete the existing animation and start fresh. When the animation is ready, tell the user to run /think-back again to play it.
```

## Prompt Translation

```text
使用 `Skill` 工具调用 `thinkback` skill，并设置 `mode=regenerate`，从头创建一个全新的 Claude Code 年度回顾动画。删除现有动画，重新开始。动画准备好后，告诉用户再次运行 `/think-back` 来播放它。
```
