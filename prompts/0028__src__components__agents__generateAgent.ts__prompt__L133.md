# prompt

- Source: `src/components/agents/generateAgent.ts`
- Symbol: `prompt`
- Line: 133
- Kind: `variable`
- Extraction: `text`

## Prompt

```text
Create an agent configuration based on this request: "${userPrompt}".${existingList}
  Return ONLY the JSON object, no other text.
```

## Prompt Translation

```text
根据此请求创建一个代理配置："${userPrompt}"。${existingList}
  仅返回 JSON 对象，不要返回其他文本。
```
