# DIRS_EXIST_GUIDANCE

- Source: `src/memdir/memdir.ts`
- Symbol: `DIRS_EXIST_GUIDANCE`
- Line: 118
- Kind: `variable`
- Extraction: `text`

## Prompt

```text
Both directories already exist — write to them directly with the Write tool (do not run mkdir or check for their existence).
```

## Prompt Translation

```text
这两个目录都已经存在——请直接使用 Write 工具写入它们（不要运行 `mkdir`，也不要检查它们是否存在）。
```
