# getCompactPrompt

- Source: `src/services/compact/prompt.ts`
- Symbol: `getCompactPrompt`
- Line: 293
- Kind: `function`
- Extraction: `source`

## Source

```ts
export function getCompactPrompt(customInstructions?: string): string {
  let prompt = NO_TOOLS_PREAMBLE + BASE_COMPACT_PROMPT

  if (customInstructions && customInstructions.trim() !== '') {
    prompt += `\n\nAdditional Instructions:\n${customInstructions}`
  }

  prompt += NO_TOOLS_TRAILER

  return prompt
}
```

## Prompt Translation

```text
关键：仅以纯文本回应。不要调用任何工具。

- 不要使用 `Read`、`Bash`、`Grep`、`Glob`、`Edit`、`Write` 或任何其他工具。
- 你已经拥有上方对话中的所有所需上下文。
- 工具调用会被拒绝，并会浪费你唯一的一次回合，你会失败。
- 你的整段回复必须是纯文本：先是一个 `<analysis>` 块，然后是一个 `<summary>` 块。

你的任务是为到目前为止的对话创建一份详细摘要，密切关注用户的明确请求以及你之前的行动。
这份摘要应当足够全面，能够捕捉技术细节、代码模式和架构决策，以便在不丢失上下文的情况下继续开发工作。

在提供最终摘要之前，请在 `<analysis>` 标签中整理你的思路，确保你覆盖了所有必要要点。分析过程中：

1. 按时间顺序分析每一条消息和对话片段。对于每个片段，全面识别：
   - 用户的明确请求和意图
   - 你为回应用户请求所采取的方法
   - 关键决策、技术概念和代码模式
   - 具体细节，例如：
     - 文件名
     - 完整代码片段
     - 函数签名
     - 文件编辑
   - 你遇到的错误以及你是如何修复它们的
   - 特别注意你收到的具体用户反馈，尤其是用户要求你以不同方式处理的情况。
2. 再次检查技术准确性和完整性，全面覆盖每一项必需内容。

你的摘要应包括以下部分：

1. 主要请求与意图：详细捕捉用户的所有明确请求和意图
2. 关键技术概念：列出讨论过的所有重要技术概念、技术和框架。
3. 文件和代码片段：列举检查、修改或创建的具体文件和代码片段。特别关注最近的消息，并在适用时包含完整代码片段，同时说明读取或编辑该文件的重要性。
4. 错误与修复：列出你遇到的所有错误以及你是如何修复它们的。特别注意你收到的具体用户反馈，尤其是用户要求你以不同方式处理的情况。
5. 问题解决：记录已解决的问题以及任何仍在进行的排查工作。
6. 所有用户消息：列出所有不属于工具结果的用户消息。这些对于理解用户的反馈和意图变化至关重要。
7. 待办任务：概述你明确被要求处理的任何待办事项。
8. 当前工作：详细描述在收到这次摘要请求之前正在进行的具体工作，特别关注来自用户和助手的最近消息。必要时包含文件名和代码片段。
9. 可选下一步：列出与你最近正在进行的工作直接相关的下一步。重要：确保这一步与用户最近的明确请求以及你在这次摘要请求前正在进行的任务直接一致。如果你的上一个任务已经结束，那么只有在与用户请求明确一致时才列出下一步。不要在没有再次确认的情况下，转向无关请求或早已完成的旧请求。

以下是你的输出应采用的结构示例：

<example>
<analysis>
[你的思路，确保所有要点都被彻底且准确地覆盖]
</analysis>

<summary>
1. 主要请求与意图：
   [详细描述]

2. 关键技术概念：
   - [概念 1]
   - [概念 2]
   - [...]

3. 文件和代码片段：
   - [文件名 1]
      - [说明这个文件为何重要]
      - [说明对这个文件所做的更改（如有）]
      - [重要代码片段]
   - [文件名 2]
      - [重要代码片段]
   - [...]

4. 错误与修复：
    - [错误 1 的详细描述]：
      - [你是如何修复它的]
      - [如果有，用户对该错误的反馈]
    - [...]

5. 问题解决：
   [对已解决问题和当前排查工作的描述]

6. 所有用户消息：
    - [详细的非工具用户消息]
    - [...]

7. 待办任务：
   - [任务 1]
   - [任务 2]
   - [...]

8. 当前工作：
   [对当前工作的准确描述]

9. 可选下一步：
   [可选的下一步]

</summary>
</example>

请基于到目前为止的对话，按照这个结构提供你的摘要，并确保表述精准且详尽。 

如果附带的上下文中还有额外的摘要说明，也请记得在生成上述摘要时遵循这些说明。例如，这些说明可能包括：
<example>
## 紧凑说明
在总结对话时，重点关注 TypeScript 代码改动，并记住你犯过的错误以及你是如何修复它们的。
</example>

<example>
# 摘要说明
在使用 compact 时，请重点关注测试输出和代码改动。包含逐字的文件读取内容。
</example>

附加说明：
${customInstructions}

提醒：不要调用任何工具。仅使用纯文本回应。先是一个 `<analysis>` 块，然后是一个 `<summary>` 块。工具调用会被拒绝，你会失败任务。
```
