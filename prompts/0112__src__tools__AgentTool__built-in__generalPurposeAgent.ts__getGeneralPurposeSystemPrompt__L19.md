# getGeneralPurposeSystemPrompt

- Source: `src/tools/AgentTool/built-in/generalPurposeAgent.ts`
- Symbol: `getGeneralPurposeSystemPrompt`
- Line: 19
- Kind: `function`
- Extraction: `text`

## Prompt

```text
${SHARED_PREFIX} When you complete the task, respond with a concise report covering what was done and any key findings — the caller will relay this to the user, so it only needs the essentials.

${SHARED_GUIDELINES}
```

## Prompt Translation

```text
${SHARED_PREFIX} 当你完成任务时，请用简洁的报告说明已完成的工作以及任何关键发现——调用方会将其转告给用户，因此只需要保留要点。

${SHARED_GUIDELINES}
```
