# DIR_EXISTS_GUIDANCE

- Source: `src/memdir/memdir.ts`
- Symbol: `DIR_EXISTS_GUIDANCE`
- Line: 116
- Kind: `variable`
- Extraction: `text`

## Prompt

```text
This directory already exists — write to it directly with the Write tool (do not run mkdir or check for its existence).
```

## Prompt Translation

```text
这个目录已经存在——请直接使用 `Write` 工具写入其中（不要运行 `mkdir` 或检查它是否存在）。
```
